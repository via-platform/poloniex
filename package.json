{
  "name": "poloniex",
  "version": "0.2.0",
  "description": "An adapter to connect Via to the Poloniex exchange.",
  "main": "./lib/main",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "via",
    "trading",
    "poloniex"
  ],
  "exchange": "poloniex",
  "repository": "https://github.com/via-platform/poloniex",
  "author": "Dakota Gordon",
  "engines": {
    "via": "*"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "underscore-plus": "^1.6.6",
    "uuid": "^3.1.0",
    "ws": "^3.3.3"
  },
  "configSchema": {},
  "accountConfigSchema": {
    "accountName": {
      "type": "string",
      "default": "Poloniex Trading Account",
      "description": "Give this account a short name for your own personal reference."
    },
    "apiKey": {
      "title": "API Key",
      "type": "string",
      "default": "",
      "description": "The API key generated by Poloniex."
    },
    "apiSecret": {
      "title": "Secret Key",
      "type": "string",
      "default": "",
      "description": "The API secret key generated by Poloniex."
    }
  }
}
